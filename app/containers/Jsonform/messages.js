/*
 * Jsonform Messages
 *
 * This contains all the text for the Jsonform component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.containers.Jsonform.header',
    defaultMessage: 'This is an example of using react-jsonschema-form library to build forms based on JSON schema.',
  },
});
