// import { fromJS } from 'immutable';
// import { makeSelectJsonformDomain } from '../selectors';

// const selector = makeSelectJsonformDomain();

describe('makeSelectJsonformDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
